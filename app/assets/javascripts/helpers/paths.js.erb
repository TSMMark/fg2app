<% environment.context_class.instance_eval { include Rails.application.routes.url_helpers } %>

window.Helper.define( "Paths", false, {
  omniauth_facebook : "<%= user_omniauth_authorize_path(:facebook) %>"
});
